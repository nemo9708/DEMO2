<UserControl x:Class="DEMO2.Manual.StationTeaching.Points.SlotSetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600"
             Background="Transparent">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BlueBrush" Color="#ADD8E6"/>
        <SolidColorBrush x:Key="OrangeBrush" Color="#F4A460"/>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border BorderBrush="Black" BorderThickness="2" Background="White" 
                HorizontalAlignment="Center" VerticalAlignment="Center" 
                Padding="20" Width="520" Height="120">

            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="Slot Count" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="20" Style="{StaticResource InputStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="Slot Pitch 1" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="20" Style="{StaticResource InputStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="Slot Pitch 2" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="30" Style="{StaticResource InputStyle}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>